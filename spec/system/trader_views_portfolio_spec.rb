require "rails_helper"

RSpec.describe "Trader views portfolio", type: :system do
  it "shows portfolio with stock rows", pending: true
  it "shows totals and P/L color badges", pending: true
  it "shows an empty state when there are no holdings", pending: true
end

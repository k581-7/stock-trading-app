require "rails_helper"

RSpec.describe "Trader views transactions", type: :system do
  it "lists transactions with date, symbol, type, shares, price, subtotal", pending: true
  it "filters by symbol", pending: true
  it "filters by type (buy/sell)", pending: true
  it "shows an empty state when there are no transactions", pending: true
end

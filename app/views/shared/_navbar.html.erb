<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-3">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold" href="/">PNM</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <!-- Left-side nav links -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"><%= link_to 'Dashboard', dashboard_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to 'Trading', new_trade_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to 'Portfolio', portfolios_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to 'Wallet', wallet_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to 'Transactions', trade_logs_path, class: 'nav-link' %></li>
      </ul>

      <!-- Right-side user info and logout -->
      <% if user_signed_in? %>
        <ul class="navbar-nav align-items-center">
          <li class="nav-item me-2">
            <span class="navbar-text text-white">
              Logged in as <strong><%= current_user.email %></strong>
            </span>
          </li>
          <li class="nav-item">
            <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'nav-link text-danger' %>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
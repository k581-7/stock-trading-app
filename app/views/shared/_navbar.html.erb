<nav class="navbar navbar-expand-lg" style="background-color: #1c1f26;">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold text-light" href="/">PNM</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <!-- Left-side nav links -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to dashboard_path, class: 'nav-link text-light' do %>
            <i class="bi bi-speedometer2 me-1 text-info"></i> Dashboard
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to new_trade_path, class: 'nav-link text-light' do %>
            <i class="bi bi-graph-up-arrow me-1 text-info"></i> Trading
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to portfolios_path, class: 'nav-link text-light' do %>
            <i class="bi bi-pie-chart me-1 text-info"></i> Portfolio
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to wallet_path, class: 'nav-link text-light' do %>
            <i class="bi bi-wallet2 me-1 text-info"></i> Wallet
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to trade_logs_path, class: 'nav-link text-light' do %>
            <i class="bi bi-journal-text me-1 text-info"></i> Transactions
          <% end %>
        </li>
      </ul>

      <!-- Right-side user info and logout -->
      <% if user_signed_in? %>
        <ul class="navbar-nav align-items-center">
          <li class="nav-item me-2">
            <span class="navbar-text text-light">
              Logged in as <strong><%= current_user.email %></strong>
            </span>
          </li>
          <li class="nav-item">
            <%= button_to destroy_user_session_path, method: :delete, class: 'nav-link text-danger' do %>
              <i class="bi bi-box-arrow-right me-1"></i> Logout
            <% end %>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>